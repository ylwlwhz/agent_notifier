{
  "name": "Code助手",
  "version": "1.0.0",
  "description": "Claude Code / Codex CLI / Cursor 飞书交互卡通知与远程控制系统。",
  "main": "hook-handler.js",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'",
    "test:core": "node --test 'tests/core/**/*.test.js'",
    "test:adapters": "node --test 'tests/adapters/**/*.test.js'",
    "test:portable": "npx -y node@22.14.0 --test 'tests/**/*.test.js'",
    "feishu-listener": "node feishu-listener.js",
    "feishu-listener:start": "nohup node feishu-listener.js >> feishu-listener.log 2>&1 & echo $! > feishu-listener.pid && echo 'Feishu listener started (PID: '$(cat feishu-listener.pid)')'",
    "feishu-listener:stop": "if [ -f feishu-listener.pid ]; then kill $(cat feishu-listener.pid) 2>/dev/null; rm feishu-listener.pid; echo 'Feishu listener stopped'; else echo 'No PID file found'; fi",
    "codex-watcher": "node src/apps/codex-watcher.js",
    "codex-watcher:start": "nohup node src/apps/codex-watcher.js >> codex-watcher.log 2>&1 & PID=$!; echo $PID > codex-watcher.pid; sleep 1; if kill -0 $PID 2>/dev/null; then echo 'Codex watcher started (PID: '$PID')'; else echo 'Codex watcher failed to stay alive; check codex-watcher.log'; tail -n 40 codex-watcher.log; exit 1; fi",
    "codex-watcher:stop": "if [ -f codex-watcher.pid ]; then kill $(cat codex-watcher.pid) 2>/dev/null; rm codex-watcher.pid; echo 'Codex watcher stopped'; else pkill -f 'node src/apps/codex-watcher.js' 2>/dev/null && echo 'Codex watcher stopped (pkill)' || echo 'No watcher process found'; fi",
    "cursor:hooks": "node scripts/setup-cursor-hooks.js",
    "cursor:hooks:notify-only": "node scripts/setup-cursor-hooks.js --notify-only",
    "cursor:hooks:remove": "node scripts/setup-cursor-hooks.js --remove",
    "cursor:mcp": "node scripts/setup-cursor-mcp.js",
    "cursor:mcp:remove": "node scripts/setup-cursor-mcp.js --remove",
    "cursor:e2e:cards": "node scripts/send-cursor-feishu-test-cards.js",
    "cursor:e2e:approval": "node scripts/send-cursor-feishu-test-cards.js --approval",
    "cursor:e2e:followup": "node scripts/send-cursor-feishu-test-cards.js --followup",
    "codex:e2e:cards": "node scripts/send-codex-feishu-test-cards.js",
    "codex:feed:text": "node scripts/send-codex-assistant-feed.js",
    "codex:send:text": "node scripts/send-codex-assistant-direct.js",
    "ask:e2e:card": "node scripts/send-claude-ask-test-card.js",
    "statusline:install": "mkdir -p \"$HOME/.claude\" && cp scripts/statusline.sh \"$HOME/.claude/statusline.sh\" && chmod +x \"$HOME/.claude/statusline.sh\" && echo '已更新 ~/.claude/statusline.sh（settings.json 接入请运行 ./install.sh）'"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "type": "commonjs",
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.60.0",
    "dotenv": "^17.2.3",
    "https-proxy-agent": "^9.1.0"
  }
}
